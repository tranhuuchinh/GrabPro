import { StyleSheet } from "react-native";
import colors from "../../styles/colors";

const styles = StyleSheet.create({
    container: {
        marginTop: 100,
        backgroundColor: colors.primary__650,
    },
    title:{
        color: '#fff',
        fontSize: 20,
        fontWeight: 500,
        textAlign: "center",
        padding: 20,
    }
});

export default styles;